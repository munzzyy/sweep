# Reproducibility first: no daemons hoarding state in CI, no cruncher, and
# dependency locking pins every resolved version (see gradle.lockfile).
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enablePngCrunchInReleaseBuilds=false
